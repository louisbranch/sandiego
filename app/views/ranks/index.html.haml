%h2 Ranks
%table
  %thead
    %tr
      %th Nome
      %th Patente
      %th Pontos
  %tbody
    - @ranks.each do |rank|
      %tr
        %td= rank.name
        %td= rank.stars
        %td= rank.minimum_xp

%h3 Ranking de Jogadores
%table
  %thead
    %tr
      %th
      %th Nome
      %th Casos
      %th Pontos
      %th Rank
  %tbody
    - @users.each_with_index do |user, i|
      %tr
        %td= i+1
        %td= user.name
        %td= user.missions.count
        %td= user.xp
        %td= user.rank.name

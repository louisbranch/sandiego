%h4 Investigar Cidade
%ul#networks_list
  -@networks.includes(:location).order('locations.name').each do |network|
    %li
      = link_to mission_network_path(@mission,network),(network.final? ? {} : {:remote => true}) do
        = image_tag(network.location.photo_path)
        %div
          %span= network.location.name
- if @time_traveled
  %div#city_time_traveled{'data-growl' => image_path("clock.png")}= "#{@time_traveled} horas de viagem"

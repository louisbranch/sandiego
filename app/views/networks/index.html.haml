%h2 Investigar Locais
%ul
  -@networks.includes(:location).order('locations.name').each do |network|
    %li= link_to network.location.name, mission_network_path(@mission,network)
= render 'shared/mission_navigation', :mission => @mission
